# Example environment variables file
# Copy this to server/.env and fill in with your actual values

# ⚠️ NEVER COMMIT REAL VALUES TO VERSION CONTROL ⚠️

# Application
NODE_ENV=development
PORT=8000
FRONTEND_URL=http://localhost:3000

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rag_db
DB_USER=rag_user
DB_PASSWORD=your-secure-db-password

# Vector Database (Qdrant)
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_NAME=rag_documents

# OpenAI API
OPENAI_API_KEY=your-openai-api-key

# JWT Configuration (Generate with: openssl rand -hex 64)
JWT_SECRET=your-64-character-hex-string
JWT_REFRESH_SECRET=different-64-character-hex-string
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Security
BCRYPT_SALT_ROUNDS=12
SESSION_SECRET=another-64-character-hex-string

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5

# File Upload
MAX_FILE_SIZE=52428800
ALLOWED_FILE_TYPES=pdf,txt,md,csv,json