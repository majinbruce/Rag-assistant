{
  "name": "rag-assistant",
  "version": "1.0.0",
  "description": "RAG Assistant Application",
  "scripts": {
    "build": "cd server && npm ci",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}